<%= form_with(model: shipping, local: true) do |form| %>
  <% if shipping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping.errors.count, "error") %> prohibited this shipping from being saved:</h2>

      <ul>
      <% shipping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :receiver_name %>
    <%= form.text_field :receiver_name %>
  </div>

  <div class="field">
    <%= form.label :receiver_phone %>
    <%= form.text_field :receiver_phone %>
  </div>

  <div class="field">
    <%= form.label :shipping_address %>
    <%= form.text_field :shipping_address %>
  </div>

  <div class="field">
    <%= form.label :shipping_day %>
    <%= form.datetime_select :shipping_day %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
